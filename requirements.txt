# Python dependencies (flask, groq, mem0, requests, etc.)

flask
flask-cors
groq
python-dotenv
mem0
requests  # For making calls to ElevenLabs and Fal.ai
# (or use specific client libraries if provided by Fal/ElevenLabs)